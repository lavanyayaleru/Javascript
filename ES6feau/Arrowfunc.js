// Before arrow func's we can use the normal funcn'sin normal func's we have to use always function keyword and func'n name in evry code.to over come this we can use the arrow functions.
// By using arrow func's we can write a code in a single line also we can decrease the lines of code.
// Both are doing the same job in this case we can use the Arrow func's.

// Normal func'n:

function add(a,b){
    return (a+b)
}
console.log(add(7,5))

// Arrow Function:

let add1=(c,d)=>(c+b)
console.log(add1(7,5))